1. python -m venv .venv
2. activate the env -  .venv\Scripts\activate
3. git initialization after creating repo in github
4. perform all the git operations
    >git init
    >git add README.md
    >git commit -m "First Commit"
    >git branch -M main
    >git remote add origin https://github.com/SambhavJn/MODEL_TESTING.git
    >git remote -v
    >git push -u origin main

5. create setup.py in main folder
6. create requirements.txt with a -e . -> this is will create model_testing.egg-info folder and will have all the information about the installation
7. create src folder in main folder
    > create __init__.py file 
8. create the folder according to the projects and then push to git as we are doing above